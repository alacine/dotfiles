# Generated by main.tf, do not edit.

%{ for server in servers ~}
${ server.name } ansible_host=${ server.ssh_host }
%{ endfor ~}


[all:vars]
ansible_connection=ssh
ansible_user=${ user }
ansible_password=${ password }
ansible_port=22
ansible_ssh_private_key_file=${ private_key_path }


[srv]
%{ for server in servers ~}
${ server.name }
%{ endfor ~}
