{
  "get self path": {
    "prefix": "sp",
    "body": ["SELF_NAME=$(readlink -f \"\\$0\")", "SELF_PATH=$(dirname \"\\${SELF_NAME}\")"]
  },
  "read multi lines": {
    "prefix": "rls",
    "body": ["while IFS=${1} read -r ${2:line}; do", "done < \"${3:input}\""]
  }
}
